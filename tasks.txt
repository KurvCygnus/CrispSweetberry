2025/10/28:
    Just focus on throwable torch.
        -Define item itself, which extends SnowBallItem class, implements ProjectileItem.
        -Spawns a spinning torch proj.
        -Once it hits, check whether it is a mob or a block.
        -If it's a mob, do 1 damage and light that guy.
        -Or if it's the later one, just place a temporary torch there.
        -In the later case, if it's underwater, it won't happen, of course.
        -Once the torch gets placed, it will start consuming its life, then become darker and darker, and eventually burn out.(How poor QAQ)
        -Texture will be slightly different from vanilla torch, as it will be shorter and the pixel of lit part is darker.